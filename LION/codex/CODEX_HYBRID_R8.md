# Codex hybrid routing extension — R8

This extension does not replace `CODEX_PROJECT_INTEGRATION.md` and grants no authority. It binds proposal routing to a shared policy digest. Deterministic work (hash/schema/test/Git observations) uses deterministic runners; bounded local-model proposal classes require a current qualified profile and one-slot budget; architecture/security/currentness/authority/cross-module work remains SaaS-reviewed. An inactive local runtime routes to SaaS/deterministic/defer, never to an assumed local executor. Context overflow escalates rather than truncates.

Current candidate evidence: `LION/architecture/v1_4/r8_hybrid/R8_HYBRID_STATE__v1_4_r8_hybrid_r1.json`.
