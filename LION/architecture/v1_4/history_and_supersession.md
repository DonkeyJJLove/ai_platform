# LION v1.4 — history, supersession and falsification boundaries

This directory is a candidate documentation projection for R20. It does not rewrite v1.3 or v14c2 history. RAG32 originals remain immutable historical evidence.

## Superseded currentness

`LION/status.json` remains a historical E003-era projection whose `CURRENT` label is stale relative to live `master`. R20 does not silently rewrite that consumer-sensitive state file. For v1.4 documentation currentness, use `current_state.json`, which is bound to exact Git identities.

## Action-plane falsification

The inherited statement that the next boundary simply lacks `RuntimeAdmissionEngine` is false: the engine is live. A bounded F009 evidence plane also constructs `RequestedRuntimeEffect`, runtime identity, admission, execution and independent reconciliation for one controlled create-only file effect. The remaining architecture frontier is the generalized reusable canonical materialization/binding from PDP ALLOW + exact proposal/current authority/provisioning to the objects consumed by the existing admission engine.

## Factory claims

The bounded B0 two-unseen-family protocol is not evidence of general Factory generativity, activated child autonomy or Factory-of-Factories. Those remain unproven.

## Federation drift during R20

`writeups` temporarily advanced to `9b77be4ba30cff15c39931717a8504c0dc877ce5` with an R18 frozen autonomy experiment package and then reverted it at current HEAD `cb7e304512566f16ed11f1a659799ec79fa5f5f6`. The current tree is `2b82b8875f976b836af04db412483ce278e1ef95`; the transient package is historical evidence only, not a current capability.
