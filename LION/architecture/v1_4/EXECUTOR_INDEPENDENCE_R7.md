# Material executor independence — R7

Four connectors are operational but all expose the same WSL2 boot id. Connector count, hostname count, PID count and logical-drone count are not material-executor evidence. Boot-domain independence is falsified; physical and control-domain independence remain NOT_PROVEN; independent material executors proven=0. Promotion requires at least two current external attestations binding distinct runtime-instance, physical-domain, control-domain and trust-anchor identities without shared-boot contradiction.
