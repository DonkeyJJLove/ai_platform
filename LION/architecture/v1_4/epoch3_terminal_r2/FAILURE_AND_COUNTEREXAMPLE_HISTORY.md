# Failure and counterexample history

After `b35642c`, the new recent-list endpoint returned a sampled 0.172-second response, but concurrent legacy cards still caused polling failures. Profiling the legacy adapter found 303 focus lookups and 305 SQLite connections for one 25-run list. Commit `d0ea856` reads the focus once per existing read batch and serializes that bounded legacy batch, reducing it to three total connections in the full run-list path. Eight concurrent legacy reads measured 0.037 seconds after repair. Old/new adapters returned exactly equal records from the same disposable production snapshot. This preserves projection semantics without a completed-result cache; the failed `b35642c` live campaign remains historical evidence.

The subsequent `3a1a1fe` live campaign demonstrated that serialization alone still queued redundant requests: a sampled recent-list response took 10.78 seconds, and the panel's initial polling did not settle. Commit `03b9e23` instead coalesces overlapping calls onto one in-flight read and retains no completed cache. Eight concurrent reads on the disposable production snapshot completed in 0.068 seconds. Tests verify shared work, isolated caller results, a fresh database change on the next call, and recovery after a failed batch. The failed `3a1a1fe` campaign remains evidence; it is not terminal acceptance.

Live deployment of `c53319b` passed health and database integrity but exposed concurrent mission-list read contention before browser acceptance. A disposable online copy of the actual 25-mission database measured eight simultaneous fresh projections at 13.07 seconds, versus 0.49 seconds with serialized reads. Commit `b85950d` serializes only the bounded recent-list projection batch with a dedicated process-local lock; it retains fresh reads, uses the same normalized projection, and neither caches authority nor holds the lifecycle writer lock. This runtime counterexample invalidates `c53319b` as a terminal candidate. The failed browser result and deployment rollback records remain historical evidence; closure still requires successor validation and live acceptance.

The final disposable Edge/service/SQLite containment campaign exercised real PAUSE and STOP requests on two synthetic verification missions without a driver loop or production database. It verified the receipt, checkpoint and changed driver state while preserving the raw phase verdict. It also exposed a missing favicon request (HTTP 404) in Mission Control. Commit `d21235e` adds an inline empty icon declaration; `ba8101d` refreshes the static package identity. This browser asset counterexample invalidates earlier candidate identities for final publication. The production effect scan is unchanged because only static HTML and the package manifest changed. The separate Windows-mounted WSL deployment preflight stopped before any effect because its Git line-ending configuration differed; using the checkout's Windows CRLF normalization is a read-only configuration correction, not a source repair.

Evidence status: source implementation at `72fbb405f5c023baa4605a53c15f1b7b96533a98`, with inventory/package reconciliation in `6724074`. This is a source description at documentation freeze. Final clean validation, exact-head CI, deployment/restart readback and merge are separately evidenced terminal gates; this document does not predict their outcome. Authority effect: NONE.

The R2 failure matrix classifies historical R1 clean-run evidence rather than replaying it as current validation. It records 2,940 run test methods, 2,921 passed, 3 failures, 10 method errors and 6 skipped. Six additional class-setup error outcomes blocked 44 declared methods. Thus 19 nonpass outcomes are not 19 failed test methods. Eighteen outcomes were production-scan pin drift; one was a stale historical expectation. Exact source corroboration and final rerun are separate obligations.

Recorded 8780 HTML called an undeclared evidenceHtml helper. Runtime/source parity therefore needs browser checks, not only HTTP 200. Previous summary/detail divergence and full DOM replacements motivated the normalized shared view and keyed refresh.

Scheduler counterexamples include equal-clock repeated selection, lost fairness after reopen, and competing receipt content/metadata. The R2 storage candidate preserves durable turns and rejects replay/conflicts; historical conflicting receipts remain evidence instead of being deleted. Phase-control counterexamples include stale generation/source/cursor and false success without durable receipt/readback.

Source-family classification preserves old probes, dirty historical variants and obsolete publication machinery. Do not import old truth/scan pins or report all non-ancestor branches as required missing code. Sources: r2-failure-matrix.json; r2-source-family-disposition.md; repository notes; focused tests.


At commit 6724074 the clean full suite ran 2,998 methods: one expected carrier-last truth failure and three class-setup errors remained. Each setup error traced to the diagnostic outside-seven count 297, inherited from 304 total surfaces. Exact inventory now has 313 surfaces; the unchanged seven-surface set leaves 306 outside it. Commit d8c0639 updates these census expectations, preserving the security obligations and UNKNOWN global status. All 19 methods in the three previously blocked classes then passed. Final truth-bound full regression remains a separate gate.

Post-publication live-test preparation found that global focus changes were coupled to registration/activation. The dedicated metadata-only focus endpoint removes that test and operator hazard. Its four tests cover no activation or unrelated table changes, exact HTTP shape, idempotence and rollback on receipt failure. SQLite fixture connections are explicitly closed so these checks also run on Windows. This source correction invalidates the earlier 496ae45 CI identity; final clean tests and CI must run again after carrier-last rebinding.

The post-deployment Edge campaign on `28aaf7c` passed connectivity projection, polling retention, historical PIN/FOCUS restoration, worker inspection, actual delayed-response ordering, durable local pending-request recovery, and zero console/page errors. Narrow-view acceptance failed: Mission Control metadata produced a 436px document at a 390px viewport; the LPCL application grid produced a 578px body. Real DOM inspection identified unconstrained flex metadata/registry labels and the auto-sized application grid item. The CSS successor bounds the grid item and wraps those labels. A temporary browser style probe produced document/body widths of 390px and no out-of-bounds elements on both panels. This probe is not final deployed acceptance; final CI, deployment, restart durability and merge remain independent gates.
