# LION AI-native roadmap — v1.4 R23 consolidated candidate

This roadmap separates integrated foundations, bounded evidence, falsified generality, active architecture frontiers and human-authority boundaries. It is a planning semantic owner, not proof of deployment, production state or authority. Default-branch identity and candidate evidence are kept explicitly separate by the v1.4 documentation plane.

## R23 consolidation state

R23 consolidates the previously parallel R21 LPCL/Process, R21/P0 TEST_ONLY materialization and R22C→R22H→R22I Action→Runtime lineages into one `ai_platform` candidate. The exact pre-documentation candidate is `5f90f1c11e9f997ed9c5e3ac1b02c6d802d15745` / tree `5dd5dc653c24bdd810faeb61f901328ad246e3a7`. Its complete repository test discovery is PASS (2410 tests), with 268 production sources, 236 effect surfaces, six raw unclassified references and zero unresolved references after taxonomy reconciliation. The production scan digest is `5f6561edcd368c2acce5f0e216bc9e21324da2913035e92ef175b7aca5b773a2`.

The process layer is now explicit: LPCL 1.0 parses to canonical `Process IR`; `ACTION_REQUIRED` transitions emit only `ActionIntentCandidate`. Process semantics neither mint authority nor bypass the existing Action IR → PDP → RuntimeAdmission → RuntimeExecution → observation → reconciliation chain.

## DELIVERED / integrated foundations

The repository contains source-backed architecture projection and code perception, Action IR/proposal, canonical PDP handoff, authority and policy-gate primitives, runtime admission, runtime execution, effect-time currentness, runtime reconciliation, executor provisioning contracts, complete/production mediation surfaces, fleet/swarm governance surfaces, Bean/CapabilityNeed/Composition/Mosaic primitives and builder-chain binding. F009 remains bounded live end-to-end evidence for one create-only file effect with independent observation/reconciliation.

R22F delivered the reusable inert Action runtime binder. R22I closes the next Action-plane frontier: the exact outputs of `bind_allowed_action_to_runtime_inputs` are now consumed by the existing governed `RuntimeAdmissionEngine` through `admit_bound_action`, which verifies canonical PDP evidence and delegates the exact binder-produced effect and runtime identity to the existing `admit` path. No alternate executor, provider, authority source, generic effect entrypoint or deployment path was added.

R22I falsification also strengthened trusted currentness with `provisioned_executor_digest`. This binds the exact `ProvisionedExecutor` and denies coherent alternate provisioning tuples rather than relying on incidental field inconsistency. The R22I regression matrix covers exact allow consumption, DENY, stale currentness, action/resource/payload/authority/mission/provisioning/runtime-identity/PDP-receipt substitution and replay. The binder and its governed-consumption layer do not mint authority and do not themselves execute effects.

`cyber_lion/contracts/model_plane_adapter.py` remains a provider-independent model-plane contract. ASTRA is a compatibility target only; ASTRA/local model runtime remains `NOT_OBSERVED`.

## ACTIVE_FRONTIER

R21 LPCL/Process and R22I binder consumption are no longer separate architecture frontiers: they coexist in the single R23 integration train and pass the exact pre-documentation repository suite. The remaining R23 frontier is procedural and empirical: freeze the reconciled noncarrier tree, compute the truth subject excluding exactly the two carriers, update carriers last, reacquire exact-head CI/census, execute a fresh TEST_ONLY runtime proof only if the combined delta requires it and current admission exists, then obtain current merge admission and read back master.

Runtime deployment, provider selection, real child activation, production deployment and authority minting remain outside this lane. R23 consolidation does not authorize any of them.

## PARTIAL / falsified / carried-forward tracks

- **Factory/autonomy:** Bean and composition primitives exist. B0 passes for two unseen families only inside its closed grammar. A deliberately out-of-grammar `float-list` family is rejected, so **general Factory generativity is falsified outside B0 rather than inferred from B0 success**. Activated child autonomy and Factory-of-Factories remain unproven.
- **Fleet/swarm:** contracts and governance/runtime trust surfaces exist; broader operational federation and independent physical failure domains are not proven.
- **Complete mediation:** contract/implementation surfaces exist; `GlobalCompleteMediation` remains `UNKNOWN` until exhaustive effect-surface evidence closes the claim.
- **Model plane:** ASTRA-compatible provider-independent request/candidate semantics are present as a non-effectful contract. No ASTRA/local runtime deployment is observed.
- **Preproduction:** entry remains blocked until isolation, rollback/recovery, currentness, mediation and physical-domain requirements are evidenced.
- **Cyber-physical:** schemas/design are not hardware capability; physical execution remains a future governed surface.

## Historical R22I source/effect and census evidence

On exact pre-documentation candidate `b4045878be064586a61efa0f3ec8be5103a5a08c`, production source count remains 260 and effect surface count remains 236. Six raw unclassified references remain visible and taxonomy reconciliation resolves all six without hiding them. The production scan digest is `c643ab174bec81dc86fde535be72230c88cfc557a2ca5596f9362db259d02724`; stable counts do not erase the R22I production-byte drift that changed this digest.

The exact b404 Full Symbol Census is PASS: 502 committed Python files, 502 parsed files, zero parse failures, 8617 symbols, 6784 public symbols and 1052 private-material symbols, with census digest `118d23e2508b0dfcab84f2cef95381d7f4cbf68b272323013174bae82d8a1835`. This is pre-documentation evidence only. Documentation and carrier commits change Git identity, so the final carrier-bound HEAD requires a new exact census run.


## R23 source/effect and census evidence

Exact pre-documentation R23 evidence is bound to `5f90f1c11e9f997ed9c5e3ac1b02c6d802d15745` / `5dd5dc653c24bdd810faeb61f901328ad246e3a7`. Relative to master, the candidate grows from 256 to 268 production sources with no removals; the 12 additions comprise the R22C census pair, R22F/R22I Action-runtime/model contracts and the R21 LPCL/Process source set. Effect surface count remains 236. Raw inventory digest is `548f7b7db610e793e6ec79c707db423bb82ca61a238c3edce5e638b2696ea79d`, reconciled inventory digest `6435a273d31f21393ac5748ce7c551a36897a8e136240483173b107292be06c4`, taxonomy reconciliation digest `5445e88512ef25352d877550482abd3dd611ea35138c546662f72ba7e29575fe` and production scan digest `5f6561edcd368c2acce5f0e216bc9e21324da2913035e92ef175b7aca5b773a2`.

The deterministic full Python symbol census on this exact pre-documentation candidate parsed 535/535 committed Python files with zero failures and 9062 symbols (7154 public; 1079 private-material). Census digest: `e559866f26b14800273e504639895048d8c923466a46609c556e1a7e5351587f`. Because documentation/carrier commits change Git identity, a final exact-head census remains mandatory after carrier-last closure.

## Federation / documentation homeostasis

The last explicit ten-repository live federation sweep remains the R22H evidence vector. R23 does not silently promote that historical sweep into a new federation observation; federation currentness remains carried historical evidence until separately reacquired.

The documentation homeostasis loop regenerates source/effect truth and census evidence without turning documentation into authority. Dynamic `CURRENT` labels without reproducible identity evidence degrade to stale/unknown.

## Physical and operational boundary

Historical host observations remain documentation evidence only. Four logical WSL2 hosts were observed in the prior host epoch, not four independent physical failure domains. Documentation cannot turn logical separation into physical independence, cannot create a signer principal and cannot instantiate an ASTRA/local model runtime.

## Longer-horizon target

The target remains a governed Bean Factory in which capability gaps can be materialized into bounded candidate components, independently verified, admitted under explicit authority and observed/reconciled after effects. Recursive child autonomy and Factory-of-Factories require new evidence and explicit authority gates; they are not implied by composition primitives, B0 bounded experiments or model compatibility contracts.
