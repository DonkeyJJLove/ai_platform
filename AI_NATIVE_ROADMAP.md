# LION AI-native roadmap — v1.4 reconciled

This roadmap separates integrated foundations, bounded evidence, the active architecture frontier and longer-horizon targets. It is a planning semantic owner, not proof of implementation, deployment or authority. Currentness is tied to the R20 evidence projection under `LION/architecture/v1_4/`.

The pre-R20 roadmap content remains preserved in Git history at the exact R20 base commit `5d5a02b37fdfff4bcbf62f455d37ce4b86080f59`; this file supersedes its live planning classification without rewriting historical commits.

## DELIVERED / integrated foundations

The repository contains source-backed architecture projection and code perception, Action IR/proposal, canonical PDP handoff, authority and policy-gate primitives, runtime admission, runtime execution, effect-time currentness, runtime reconciliation, executor provisioning contracts, complete/production mediation surfaces, fleet/swarm governance surfaces, Bean/CapabilityNeed/Composition/Mosaic primitives and builder-chain binding. F009 additionally provides bounded live end-to-end evidence for one create-only file effect with independent observation/reconciliation.

These labels mean code/contract integration at the exact R20 baseline. They do not mean production deployment or universal coverage.

## ACTIVE_FRONTIER

The first genuinely unfinished general Action-plane dependency is a reusable canonical materializer/binder:

`PDPResult(ALLOW) + exact ActionProposal/context + current authority/currentness + trusted provisioning`

→ exact `RequestedRuntimeEffect` + `RuntimeIdentityBinding` + `CanonicalPDPDecisionEvidence`

→ existing `RuntimeAdmissionEngine.admit(...)`.

F009 proves one bounded construction path; it does not replace the missing generalized reusable boundary. The next work must preserve authority attenuation, exact identity/effect binding, replay protection and fail-closed currentness.

## PARTIAL / carried-forward tracks

- **Factory/autonomy:** primitives and bounded B0 candidate protocols exist; general generativity, activated child autonomy and Factory-of-Factories are not proven.
- **Fleet/swarm:** contracts and governance/runtime trust surfaces exist; broader operational federation and independent physical failure domains are not proven.
- **Complete mediation:** contract/implementation surfaces exist; `GlobalCompleteMediation` remains `UNKNOWN` until exhaustive effect-surface evidence closes the claim.
- **Model plane:** routing/design work may exist, but R20 observed no local model runtime on the four checked logical hosts.
- **Preproduction:** entry remains blocked until isolation, rollback/recovery, currentness, mediation and physical-domain requirements are evidenced.
- **Cyber-physical:** schemas/design are not hardware capability; physical execution remains a future governed surface.

## Physical and operational boundary

R20 observed four logical WSL2 hosts. This is not evidence of four physical failure domains. Docker and the GitHub runner were observed on `LION-AUTH-LAB`; no `lion-authority-signer` principal and no Ollama/vLLM/LLama model service were observed on the checked nodes. These observations are documentation evidence only and grant no host/runtime authority.

## Documentation homeostasis track

Every future epoch should regenerate/reconcile exact-baseline current-state and federation projections, material object/capability/contract catalogs, semantic-owner routing, contradiction/falsification/supersession registers and roadmap status. A dynamic `CURRENT` claim without reproducible baseline evidence must degrade to stale/unknown rather than being manually refreshed.

## Longer-horizon target

The target remains a governed Bean Factory in which capability gaps can be materialized into bounded candidate components, independently verified, admitted under explicit authority and observed/reconciled after effects. Recursive child autonomy and Factory-of-Factories require their own evidence and authority gates; they are not implied by composition primitives or bounded experiments.
